enum UserRoles {
  SUPERADMIN
  ADMIN
  CLIENT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
  PENDING
}

enum PlanTier {
  BASIC
  STANDARD
  PREMIUM
}

enum HelpRequestStatus {
  OPEN
  IN_PROGRESS
  RESOLVED
  CLOSED
}