enum UserRoles {
  SUPERADMIN
  ADMIN
  CLIENT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
  PENDING
}

enum PlanTier {
  FREE
  BASIC
  STANDARD
  PREMIUM
}

enum HelpRequestStatus {
  OPEN
  IN_PROGRESS
  RESOLVED
  CLOSED
}

enum DisputeStatus {
  OPEN
  UNDER_REVIEW
  RESOLVED
  REJECTED
  CLOSED
}

enum DisputeCategory {
  PAYMENT
  BOOKING
  REFUND
  SERVICE
  ACCOUNT
  OTHER
}

enum DisputePriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum BookingType {
  ONE_WAY
  ROUND_TRIP
  MULTI_CITY
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
}
